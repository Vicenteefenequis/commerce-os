## 1. Server Actions

- [x] 1.1 Add `login` Server Action calling `backendFetch("/auth/login", ...)`, returning a typed `{ error }` result on failure and redirecting on success; verify manual login succeeds and shows an inline error on bad credentials (`apps/web/app/login/actions.ts`; forwards Set-Cookie via new `applySetCookie` helper in `backend-fetch.ts`; typecheck+lint clean; end-to-end manual verification deferred to task 3.1 once the login page is wired to this action)
- [x] 1.2 Add `createVenue` Server Action calling `backendFetch("/venues", { method: "POST" })` and `revalidatePath` on the venues page; verify a new venue appears in the list without a client re-fetch (`apps/web/app/venues/actions.ts`; typecheck+lint clean; end-to-end verification deferred to task 3.2)
- [x] 1.3 Add `createProduct` Server Action calling `backendFetch("/products", { method: "POST" })` and `revalidatePath` on the products page; verify a new product appears in the list (`apps/web/app/products/actions.ts`; typecheck+lint clean; end-to-end verification deferred to task 3.3)
- [x] 1.4 Add `createResource` Server Action calling `backendFetch("/resources", { method: "POST" })` and `revalidatePath` on the resources page; verify a new resource appears in the list (`apps/web/app/resources/actions.ts`; typecheck+lint clean; end-to-end verification deferred to task 3.4)
- [x] 1.5 Add `updateResourceCapacity` Server Action calling `backendFetch("/resources/{id}/capacity", { method: "PATCH"/"PUT" as backend expects })` and `revalidatePath`; verify capacity dialog submit updates the displayed value (`apps/web/app/resources/actions.ts`; uses PUT per existing backend contract; typecheck+lint clean; end-to-end verification deferred to task 3.4)
- [x] 1.6 Add `setVariantPrice` Server Action calling `backendFetch("/products/{id}/variants/{variantId}/price", { method: "PUT" })` and `revalidatePath` on the products page; verify the price dialog submit updates the displayed price (gap found while implementing 1.3 — not in the original proposal/tasks; covered by `apps/web/app/api/products/[id]/variants/[variantId]/price/route.ts` today) (`apps/web/app/products/actions.ts`; typecheck+lint clean; end-to-end verification deferred to task 3.3)
- [x] 1.7 Add `checkResourceAvailability` Server Action calling `backendFetch("/resources/{id}/available-capacity?period=...")` and returning the result to the client leaf; verify the availability dialog submit shows the queried value (gap found while implementing 1.5 — not in the original proposal/tasks; covered by `apps/web/app/api/resources/[id]/available-capacity/route.ts` today; this is a read, not a mutation, but it is user-triggered on-demand rather than initial page data, so it follows the same "no client fetch to /api/*" policy as writes) (`apps/web/app/resources/actions.ts`; typecheck+lint clean; end-to-end verification deferred to task 3.4)

## 2. Server-side reads

- [x] 2.1 Convert `venues` page to a Server Component that calls `backendFetch("/venues")` directly and passes the list as props to a client leaf; verify the venue list renders with data in the initial HTML (no loading flicker on load) (`apps/web/app/venues/page.tsx` now a Server Component; UI/mutation logic extracted to `venues-content.tsx`; `next build` marks `/venues` as `ƒ` server-rendered; mutation flow still uses the old `/api/venues` route handler for create+refresh, replaced in task 3.2)
- [x] 2.2 Convert `products` page to a Server Component calling `backendFetch("/products?venueId=...")` (search params from the page's own `searchParams`) and passing venues + products as props; verify list renders with data in the initial HTML (`apps/web/app/products/page.tsx`; venue selection now navigates via `?venueId=` search param instead of a client fetch-on-change, which also removes the client fetch that used to run when switching venues; UI/mutation logic extracted to `products-content.tsx`; `next build` marks `/products` as `ƒ`)
- [x] 2.3 Convert `resources` page to a Server Component calling `backendFetch("/resources?venueId=...")` and passing venues + resources as props; verify list renders with data in the initial HTML (`apps/web/app/resources/page.tsx`; same `?venueId=` search-param pattern as products; UI/mutation logic extracted to `resources-content.tsx`; `next build` marks `/resources` as `ƒ`)

## 3. Client leaves rewired to Server Actions

- [x] 3.1 Rewire login form to call the `login` Server Action (form `action` or `useActionState`) instead of `fetch("/api/auth/login")`; verify no client-side `fetch` remains in `login/page.tsx` (imperative call to the action from the existing `onSubmit` handler; success path relies on the action's own `redirect("/venues")`; `grep fetch(` in `app/login` returns nothing; typecheck/lint/build clean)
- [x] 3.2 Rewire venue create dialog/form to call `createVenue`; verify no client-side `fetch` remains in `venues/page.tsx` (`venues-content.tsx` now renders `venues` straight from Server Component props — no local mirrored state — since `createVenue`'s `revalidatePath` refreshes props automatically; `grep fetch(` in `app/venues` returns nothing)
- [x] 3.3 Rewire product create dialog/form to call `createProduct`; verify no client-side `fetch` remains in `products/page.tsx` (also rewired the price dialog to `setVariantPrice` from task 1.6, since it's the same client leaf; `products-content.tsx` renders `products` from props; `grep fetch(` in `app/products` returns nothing)
- [x] 3.4 Rewire resource create dialog/form and capacity dialog to call `createResource`/`updateResourceCapacity`; verify no client-side `fetch` remains in `resources/page.tsx` (also rewired the availability dialog to `checkResourceAvailability` from task 1.7, since it's the same client leaf; `resources-content.tsx` renders `resources` from props; `grep fetch(` in `app/resources` returns nothing)

Verification performed: `tsc --noEmit`, `eslint`, and `next build` all clean after each change; `grep -rn "fetch(" app/login app/venues app/products app/resources` returns no matches. True manual click-through (login → create → see it appear) was NOT performed — it requires a running backend + seeded Postgres DB, which is not set up in this session. Recommend a manual smoke test before merging.

## 4. Cleanup

- [x] 4.1 Delete `apps/web/app/api/auth/`, `apps/web/app/api/venues/`, `apps/web/app/api/products/`, `apps/web/app/api/resources/` route handlers; verify `grep -r "/api/auth/login\|/api/venues\|/api/products\|/api/resources" apps/web` finds no remaining callers (only matches left are generated `.next/types/*` build artifacts, which regenerate on next build; `/api/health` and `/api/organizations/[id]` untouched — out of scope for this change)
- [x] 4.2 Run `apps/web` typecheck, lint, test, build scripts and verify all pass — **partially blocked**: `apps/web/package.json` has no `typecheck` or `test` script (only `dev`, `build`, `start`, `lint`), despite `nextjs-frontend-conventions` SKILL.md requiring both. This predates this change — ran the equivalent commands directly instead: `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean, confirms deleted routes have no remaining references). No test suite exists to run. Flagging rather than adding a test framework or scripts myself, since that's separate scope from this change.

## 5. Policy documentation

- [x] 5.1 Add a "Data fetching" section to `.claude/skills/nextjs-frontend-conventions/SKILL.md` codifying: reads via Server Components + `backendFetch`, writes via Server Actions, `"use client"` scoped to interactive leaves receiving data via props, no client `fetch` to internal `/api/*` for page data (replaced the old "API access" section, which referenced a `src/lib/api-client.ts` module that doesn't exist in this codebase; folded its still-valid `ApiError` guidance into the new section)
